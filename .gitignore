# macOS
.DS_Store
.AppleDouble
.LSOverride

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Often good to commit, but can be ignored in some monorepo setups if managed by a root lockfile. For now, let's add it and can be removed if you use a root package-lock.json
yarn.lock # Same as above

# IDEs and editors
.idea/
.vscode/
*.swp
*.swo

# Build output
dist/
build/
out/

# Logs
logs/
*.log

# Environment variables
.env
.env.local
# .env*.local (this was too broad, let's be more specific)
.env.development.local
.env.test.local
.env.production.local

# Allow template/default env files for different environments (usually no secrets here)
# .env.development
# .env.production

# Other specific sensitive files
email-assistant-backend/other_env.txt 